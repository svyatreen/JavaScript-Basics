const myName = 'Svyatoslav';
console.log('myName', myName, typeof myName);

const myAge = 19;
console.log('myAge', myAge, typeof myAge);

const carOwner = undefined;
console.log('carOwner', carOwner, typeof carOwner);

const iHaveCar = false;
console.log('iHaveCar', iHaveCar, typeof iHaveCar);

const bigNumber = 200n;
console.log('bigNumber', bigNumber, typeof bigNumber);

const myFavoriteDrink = null;
console.log('myFavoriteDrink', myFavoriteDrink, typeof myFavoriteDrink);

const id = Symbol('id');
console.log('id', id, typeof id);

const car = {
  carName: 'Porsche',
  carSpeed: 300,
  carComfortable: true,
};
console.log('car', car, typeof car);
